#group-box
  = link_to group_messages_path(grp.id) do
    #group-box__name
      = grp.name
    #group-box__message
      - if grp.messages.last.present?
        = grp.messages.last.body
      - else
